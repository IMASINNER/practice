spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mytest?useUnicode=true&characterEncoding=utf-8&useSSL=false
    type: com.alibaba.druid.pool.DruidDataSource
    username: root
    password: 123456
      ##驱动包
    driver-class-name: com.mysql.jdbc.Driver
  flyway:
    enabled: true
    encoding: UTF-8
    baseline-on-migrate: true
  #启动或禁用flyway
jpa:
  hibernate:
    ##自动更新
    #ddl-auto: update
    ##自动打印sql
  show-sql: true





mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.cyzc.test.Entity
##mybatis打印sql语句
logging:
  level:
    com:
      cyzc:
        test:
          Dao: debug
